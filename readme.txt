This is a README file for the repository.

Why use shell scripts? For automation instead of having to do things manually.

Some shells might require packages to be installed, so please look out for comments mentioning the required packages to install in order to use the script properly.
